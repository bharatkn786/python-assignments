# Lab 05 – SQL Fundamentals

## Overview

This lab focuses on learning and applying fundamental SQL concepts using the Chinook SQLite sample database. The lab covers querying relational data, filtering records, sorting results, performing aggregations, grouping data, joining related tables, and working with date-based queries.

---

## Problem Statement

The objective of this lab was to use SQL to answer practical questions from the Chinook relational database. The tasks involved retrieving customers from a specific country, identifying the most expensive tracks, calculating revenue by country, finding the best-selling tracks, and analyzing monthly revenue for a selected year.

---

## Objectives

- Understand and practice fundamental SQL queries.
- Retrieve and filter records using `SELECT` and `WHERE`.
- Sort and limit query results using `ORDER BY` and `LIMIT`.
- Perform calculations using aggregate functions such as `SUM()`.
- Group records using `GROUP BY`.
- Join related tables using `JOIN`.
- Perform date-based analysis using SQLite date functions.
- Save SQL queries in separate `.sql` files.
- Verify SQL queries using SQLite.
- Document query results using screenshots.

---

## Technologies Used

- Ubuntu Linux / WSL
- SQLite 3
- Chinook SQLite Database
- Visual Studio Code
- Bash Terminal
- SQL
- Git
- GitHub

---

## Database Used

The **Chinook SQLite sample database** was used for this lab.

The database contains relational tables including:

- Customer
- Invoice
- InvoiceLine
- Track
- Album
- Artist
- Genre
- Employee
- MediaType
- Playlist
- PlaylistTrack

The database was opened and tested using SQLite.

The available tables were verified using:

```text
.tables
```

---

## Implementation

### Step 1: Set Up the Chinook Database

The Chinook SQLite sample database was downloaded and opened using SQLite.

The available tables were verified using:

```text
.tables
```
<img width="1587" height="211" alt="image" src="https://github.com/user-attachments/assets/897ce7ae-ec35-47f5-9584-65f327e9c25a" />


The database contains tables for customers, invoices, invoice items, tracks, artists, albums, playlists, and other related entities.

---

### Step 2: Customers by Country

Customers from the **USA** were retrieved using the `WHERE` clause.


<img width="1369" height="416" alt="Screenshot 2026-08-09 222448" src="https://github.com/user-attachments/assets/0239b279-ff9b-4a96-a160-209cdeaacd6d" />



The query successfully returned customers belonging to the USA.

**SQL File:** `01_customer_by_country.sql`
Solution:
<img width="1902" height="406" alt="Screenshot 2026-08-09 234657" src="https://github.com/user-attachments/assets/d1020b78-1375-4c24-8bc2-c052558c13be" />



---

### Step 3: Most Expensive Tracks

The 10 tracks with the highest unit prices were identified using `ORDER BY` and `LIMIT`.

<img width="1065" height="338" alt="Screenshot 2026-08-09 232421" src="https://github.com/user-attachments/assets/e0c57c70-3ebd-43ec-bd34-4e2767dadbd0" />


The query sorts the tracks by their unit price in descending order and returns the top 10 results.

**SQL File:** `02_most_expensive_tracks.sql`
Solution:
<img width="1465" height="471" alt="Screenshot 2026-08-09 224014" src="https://github.com/user-attachments/assets/ba633e88-8b9f-4740-ad0d-8ff5ed9326f8" />



---

### Step 4: Revenue by Country

Total revenue for each country was calculated by joining the `Customer` and `Invoice` tables.

<img width="1082" height="396" alt="Screenshot 2026-08-09 232431" src="https://github.com/user-attachments/assets/5550d924-e7a9-4bb1-b883-35c583454d00" />


The query uses `JOIN`, `SUM()`, and `GROUP BY` to calculate the total revenue generated by customers from each country.



**SQL File:** `03_revenue_by_country.sql`
Solution:
<img width="1202" height="777" alt="image" src="https://github.com/user-attachments/assets/50e4e853-d71a-4185-b399-401024004b36" />




---

### Step 5: Best-Selling Tracks

The 10 best-selling tracks were identified based on the total quantity sold.

<img width="1153" height="437" alt="Screenshot 2026-08-09 232442" src="https://github.com/user-attachments/assets/95850247-1b30-40d4-b481-85005aaa9d03" />


The query joins the `InvoiceLine` and `Track` tables and calculates the total quantity sold for each track.

The results were sorted in descending order and limited to the top 10 tracks.

**SQL File:** `04_best_selling_tracks.sql`

<img width="946" height="576" alt="Screenshot 2026-08-09 230819" src="https://github.com/user-attachments/assets/ec57b33f-f8ae-4570-b6bc-88b42ca6975b" />



---

### Step 6: Monthly Revenue for 2010

Monthly revenue for the year **2010** was calculated using SQLite's `strftime()` date function.

<img width="1627" height="182" alt="image" src="https://github.com/user-attachments/assets/44db9832-5e8d-49ef-b0b8-9b9bcfbb7f6b" />


The `strftime()` function extracts the month from the invoice date. The `SUM()` function calculates the total revenue, while `GROUP BY` groups the revenue by month.

The query returned the revenue generated for each month of 2010.

**SQL File:** `05_monthly_revenue_2010.sql`

<img width="886" height="463" alt="Screenshot 2026-08-09 231934" src="https://github.com/user-attachments/assets/3baef776-3809-4af5-b064-bd1c4801940b" />



---

## SQL Files

The SQL queries are organized in the `sql` directory:

```text
sql/
├── 01_customer_by_country.sql
├── 02_most_expensive_tracks.sql
├── 03_revenue_by_country.sql
├── 04_best_selling_tracks.sql
└── 05_monthly_revenue_2010.sql
```

---

## Screenshots

### Database Tables

The Chinook database was successfully opened and its available tables were verified using SQLite.


### Customers by Country

The query was executed successfully and returned customers from the USA.


### Most Expensive Tracks

The query returned the 10 tracks with the highest unit prices.


### Revenue by Country

The query calculated and displayed total revenue for each country.


### Best-Selling Tracks

The query returned the 10 tracks with the highest total quantity sold.


### Monthly Revenue for 2010

The query calculated the total revenue generated during each month of 2010.

---

## SQL Concepts Practiced

- `SELECT`
- `WHERE`
- `ORDER BY`
- `LIMIT`
- `SUM()`
- `GROUP BY`
- `JOIN`
- `strftime()`
- Date filtering
- Aggregate functions
- Relational database queries

---

## Conclusion

This lab provided practical experience with fundamental SQL concepts using the Chinook relational database. By completing the five queries, I practiced filtering, sorting, aggregation, grouping, table joins, and date-based analysis.

The lab also improved my understanding of how related tables can be combined to extract meaningful information such as revenue by country and best-selling tracks. Working with SQLite through the command line also provided practical experience with database management and SQL execution.

Finally, the SQL queries were organized into separate files and managed using Git and GitHub, providing experience in maintaining and documenting database-related work in a version-controlled project.
